# Examples

This directory contains curated example artifacts for repository visitors.

Use these files to understand the expected shape of SlidesMentor outputs without mixing them with local run artifacts.

Local runs should write to `output/`, which is ignored by git.
